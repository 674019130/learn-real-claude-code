# 100+ 斜杠命令的调度系统

> 命令是用户发起的 UI 操作；工具是模型发起的 API 操作。

## 概述

Claude Code 拥有超过 100 个斜杠命令：`/compact`、`/memory`、`/review`、自定义用户命令等。这些命令和工具看起来类似，但在架构上有根本区别——它们存在于不同的层次，服务于不同的目的，由不同的机制触发。

理解这个区别，能帮你设计出更清晰的 agent 界面。什么应该是命令，什么应该是工具？混淆这两个概念会导致权限管理混乱、用户体验割裂，以及难以调试的行为。

本章深入 `commands.ts` 的注册模式，解析命令系统的调度流程，并探索命令与工具如何协作。

## 命令 vs 工具：根本区别

```
命令（Commands）              工具（Tools）
─────────────────────         ──────────────────────
用户通过 /语法 触发            模型通过 API 调用触发
在 UI 层处理                   在 agent 循环中处理
有访问完整 UI 上下文的权限      只能访问工具 context
可以修改 UI 状态                修改对话历史
不出现在模型的 context 中       结果追加到消息历史
```

这个区分的实际意义：当用户输入 `/compact` 时，这个操作永远不会"进入" AI 模型——它在 UI 层被拦截处理。相反，当模型决定调用 `Read` 工具读取文件时，这发生在 agent 循环内部，用户可能甚至不知道发生了什么。

## commands.ts：注册模式

每个命令是一个包含 `name`、`description` 和 `handler` 的对象：

```typescript
// commands.ts（简化结构）
interface Command {
  name: string;
  description: string;
  aliases?: string[];
  isBuiltIn: boolean;

  // handler 接收解析后的参数和完整的 UI context
  handler(
    args: string,
    context: CommandContext
  ): Promise<CommandResult>;
}

interface CommandContext {
  messages: Message[];
  setMessages: (messages: Message[]) => void;
  currentInput: string;
  setInput: (input: string) => void;
  tools: Tool[];
  // 完整的 UI 状态访问
  ui: UIState;
}
```

注意 `CommandContext` 包含 `setMessages`——命令可以直接修改对话历史。这是工具无法做到的：工具只能返回结果，这个结果随后被追加到历史；但命令可以重写整个历史，这正是 `/compact` 压缩对话所需要的能力。

## 命令注册：内置 + 自定义

内置命令在 `commands.ts` 中静态注册：

```typescript
// 内置命令示例
const builtinCommands: Command[] = [
  {
    name: 'compact',
    aliases: ['c'],
    description: '压缩对话历史以释放 context 空间',
    isBuiltIn: true,
    async handler(args, context) {
      const summary = await summarizeConversation(context.messages);
      context.setMessages([
        { role: 'user', content: '[对话已压缩]' },
        { role: 'assistant', content: summary },
      ]);
      return { type: 'success', message: '对话已压缩' };
    },
  },

  {
    name: 'memory',
    description: '查看或编辑持久化记忆',
    isBuiltIn: true,
    async handler(args, context) {
      if (args === 'edit') {
        await openMemoryEditor();
      } else {
        return { type: 'display', content: await loadMemory() };
      }
    },
  },
];
```

自定义用户命令从 `.claude/commands/` 目录加载：

```typescript
// 加载用户自定义命令
async function loadUserCommands(): Promise<Command[]> {
  const commandFiles = await glob('.claude/commands/*.md');

  return commandFiles.map(file => ({
    name: path.basename(file, '.md'),
    description: extractDescription(file),
    isBuiltIn: false,
    async handler(args, context) {
      // 读取 .md 文件作为 prompt，注入到对话
      const prompt = await readFile(file);
      const expanded = expandVariables(prompt, { args, ...context });
      // 将自定义命令的 prompt 作为用户消息注入
      await context.sendMessage(expanded);
    },
  }));
}
```

这是一个精妙的设计：自定义命令本质上是存储在文件中的 prompt 模板。用户不需要写 TypeScript 代码就能创建新命令——只需写一个 Markdown 文件，命令系统负责其余的一切。

## 调度：从斜杠到处理器

当用户输入内容时，调度流程如下：

```
用户输入 "/compact old messages"
         │
         ▼
    前缀检查：以 "/" 开头？
         │ 是
         ▼
    解析命令名和参数
    name = "compact"
    args = "old messages"
         │
         ▼
    查找命令注册表
    commands.find(c => c.name === name || c.aliases?.includes(name))
         │
         ▼
    找到命令？
    ├── 是 → 执行 handler(args, context)
    └── 否 → 提示未知命令，列出相似命令
```

关键在于：这个调度发生在 React 组件层，在消息发送给 AI 之前。匹配到命令的输入**永远不会**发送给模型。

## 命令内部调用工具

命令和工具不是互斥的——命令可以在内部调用工具：

```typescript
// /review 命令内部使用工具完成任务
{
  name: 'review',
  async handler(args, context) {
    // 命令触发一个子 agent 任务
    const results = await runSubAgent({
      task: `Review the changes in ${args || 'current branch'}`,
      tools: [
        new BashTool(),    // git diff
        new ReadFileTool(), // 读取相关文件
        new GrepTool(),    // 搜索相关代码
      ],
      messages: [],
    });

    return { type: 'display', content: results.summary };
  },
}
```

这展示了 agent 系统的层次结构：用户命令 → 命令处理器 → 子 agent → 工具。每一层都有清晰的职责边界。

## 关键收获

- 命令在 UI 层由用户触发，永远不进入 AI 模型的 context；工具在 agent 循环中由模型触发
- `CommandContext` 包含 `setMessages`，允许命令直接修改对话历史——这是工具无法做到的
- 自定义命令通过 `.claude/commands/*.md` 文件定义，本质上是存储在磁盘上的 prompt 模板
- 调度器在发送消息给模型前拦截斜杠输入，匹配命令注册表
- 命令可以在内部实例化工具和子 agent，形成"命令 → agent → 工具"的层次结构
